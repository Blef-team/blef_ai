"""Action-abstraction package for the Blef CFR trainer.

Holds the probability kernel (`probs`) and, as they are built, the menu/resolver
machinery that turns the full 88-claim action space into a small per-infoset
slot menu (checkpoints + value/bluff abstract slots + minimal raises + check).
"""
